"""Mobile app testing engine for blop-mcp (iOS + Android via Appium 3).

Requires: pip install blop-mcp[mobile]

This package is an additive layer on top of the existing web engine.
All existing web flows are unaffected.
"""
